name: Script Runner
on:
  workflow_dispatch:
    inputs:
      script:
        required: true
        type: string
jobs:
  run-script:
    runs-on: ubuntu-latest
    steps:
      - run: ${{ inputs.script }}